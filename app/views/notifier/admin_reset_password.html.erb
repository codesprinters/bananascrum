Hello <%= @user.first_name %>,

Admin of the "<%= @user.domain.full_name %>" account in Banana Scrum has requested password reset for your account (<%= @user.login %>). Your old password has been disabled and will not work anymore.

To set your new password please click the following link:
<%= new_profile_password_url(:key => @key, :host => ::AppConfig.banana_domain) %>

If you can't click the link above, go to page:
<%= new_profile_password_url(:host => ::AppConfig.banana_domain) %>
and paste this verification key into the form:
<%= @key %>

<%= mail_footer %>